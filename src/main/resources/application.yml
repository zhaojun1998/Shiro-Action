spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3306/shiro_action?useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    im.zhaojun.mapper: debug
    im.zhaojun.controller: debug